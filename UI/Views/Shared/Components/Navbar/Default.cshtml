<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark position-relative">
    <div class="container">
        <a class="navbar-brand" asp-controller="Home" asp-action="Index">Blog</a>
        <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse" id="navbarCollapse" style="">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" asp-controller="Home" asp-action="Index">Anasayfa</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" asp-controller="Article" asp-action="Index">Makaleler</a>
                </li>
            </ul>
            <form class="d-flex">
                @if (User.Identity.IsAuthenticated)
                {
                    @await Component.InvokeAsync("UserMenu")
                }
                else
                {
                    <div class="text-end">
                      <a class="btn btn-outline-light me-2" asp-action="Login">Giriş Yap</a>
                      <a class="btn btn-warning" asp-action="Register">Kayıt Ol</a>
                    </div>
                }
            </form>
        </div>
    </div>
</nav>